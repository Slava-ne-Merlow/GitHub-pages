## Описание
dev branch
### Дедлайн всего ДЗ - 22.10.2025 в 10:00 мск

Необходимо запушить в этот репозиторий последние изменения по вашему todo-list  

Т.к. это модульный проект, то в данном случае переходим от формата  
"отдельные репозитории для каждого ДЗ" к "ДЗ через merge requests":

1. в этот репозиторий нужно залить версию todo-list, какой она была на момент субботы 11.10.2025  
   (просто запушить в master ветку вашего fork-а)

2. переписать код todo-list на typescript + настроить сборку и запуск

---

## Что сделать

### Основная часть (обязательно):

- Весь ранее реализованный функционал из переведён на `.ts`.  
- Проект компилируется **без ошибок** (`tsc`), страница работает как раньше (поведение сохранено).

### Бонусное задание (по желанию)

**Классовая архитектура по фичам**  
- Для **каждой** реализованной ранее фичи есть **отдельный класс**
- У классов есть чёткий публичный API (методы), приватное состояние, инициализация — из `main.ts`.  
- Файлы в `src/` по смысловым папкам: `features/`, `services/`, `app/` (или аналогично).

## Требования

### 0) Общие требования (и для DEV и для HW-JS)
0.1) в readme должно быть описаны предварительные действия для открытия сайта, если они есть
0.2) в readme должно быть описано как именно запускать сайт (нужны ли какие то спец.действия)
0.3) при открытии весь функционал должен быть в рабочем состоянии, в консоли не должно быть ошибок  
0.4) описать в readme кратко, что именно пользователь может делать/увидеть на сайте и то, на что вы хотите, чтобы он обратил внимание (например, какой-то крутой визуал или интересный функционал)  
**0.5) в readme должно быть описано как именно запускать сайт (надо ли предварительно запускать сборку и т.д.)**  
**0.6) в проекте обязательно должы быть `package.json`, `package-lock.json`, `.nvmrc` и `.gitignore` (как минимум для `node_modules` и  служебных/системных директорий)**  
**0.7) в консоли не должно быть ошибок по js/ts (изначально и при использовании сайта)**

### 1) JavaScript реализация (ветка DEV)
*плюс общие требования*  
1.1) желательно, чтобы тут были только javascript в чистом виде (если что-то дополнительно, то описать)  
1.2) если хочется сделать какие-то доработки или улучшения по javascript, то лучше в этой ветке  
1.3) любой рефакторинг javascript (если только это не для ts) желательно тоже в этой ветке

### 2) TypeScript реализация (ветка HW-TS)  
*плюс общие требования*  
1.1) желательно, чтобы тут были только typescript в чистом виде (если что-то дополнительно, то описать)  
1.2) если хочется сделать какие-то доработки или улучшения по typescript, то лучше в этой ветке  
1.3) любой рефакторинг typescript желательно тоже в этой ветке  
**1.4) проект должен собираться, должна работать компиляция из typescript в javascript**  
**1.5) результат сборки это папка `dist/` которая лежит на одном уровне вложенности с `src/` (`dist/` также указать в `.gitignore`)**  
**1.6) обязательно и критично наличие файла `tsconfig.json` + работает сборка + в readme в этой ветке описан смысл каждого параметра в tsconfig и смысл его значения (и почему именно это значение)**  
